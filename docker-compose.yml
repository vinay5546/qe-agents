version: "3.8"

networks:
  qe-agents-net:
    driver: bridge

services:
  sut:
    build:
      context: .
      dockerfile: docker/Dockerfile.sut
    networks:
      - qe-agents-net
    ports:
      - "4000:4000"
